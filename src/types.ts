export enum Event {
    Del = 'del',
    Enter = 'enter',
    Digit = 'digit',
}

export enum Arabic {
    Zero = 0,
    One,
    Two,
    Three,
    Four,
    Five,
    Six,
    Seven,
    Eight,
    Nine,
}
